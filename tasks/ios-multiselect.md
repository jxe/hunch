# iOS multiselect

* The swipe that currently triggers indent should instead trigger selection mode, where tapping a row adds/removes it from the selection. A floating toolbar should appear at the bottom in this mode to allow bulk actions (delete, indent, outdent, etc), including "Turn into" which could make a bunch of blocks the same type at once, or turn an indented list selection into a toggle.

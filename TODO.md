## Possibly soon

- Round the `style` tags corners
- Clamshell: file names that are automatically created should include an extra UUID (not just "Untitled") or something
- The swipe action stuff could be full bleed

## Coming later

- 🗂️  Category-style list rendering
- 🔎  Full-text search across the workspace
- We might want to track which pages are reachable and unreachable, and which have been edited recently / Each page could know its ancestors. When you mention a page, it gets added as an ancestor or something like that. That way deletions and restores of pages can support containment like Notion.

## Maybe

- 🎨  Color (text and `style`-tag backgrounds)
- On-device LLM could be used to clean up the audio transcripts (or perhaps there’s some setting for the speech transcription API that makes it do a better job / gets rid of the weird punctuation, etc)

# Probably never

- ❌  Real-time multiplayer collaboration
- ❌  Full databases (relations, formulas, rollups, views)
- ❌  Tables
- ❌  Embeds besides just fancy external links
- ❌  AI features
